# Railway 배포 설정
# n8n 프로덕션 환경용

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# 헬스체크 설정
healthcheckPath = "/healthz"
healthcheckTimeout = 300
startCommand = "n8n start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# 리소스 설정 (Hobby 요금제 최적화)
numReplicas = 1
