# Railway 배포 설정
# n8n 프로덕션 환경용

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# healthcheck 제거 - n8n 시작 시간이 오래 걸림
startCommand = "n8n start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
numReplicas = 1